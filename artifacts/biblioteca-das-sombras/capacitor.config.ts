import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appName: 'Biblioteca das Sombras',
  appId: 'com.bibliotecadassombras.app',
  webDir: 'dist/public',
};

export default config;